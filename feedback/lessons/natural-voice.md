# natural-voice — lessons

Injected at the top of every run of this skill, so keep it short: entries are ordered most-seen
first and the file is capped at 2 KB. Written only by `tools/friction.py compact`, from reviewed
inbox entries — never by hand mid-run. Format and redaction rule: `feedback/README.md`.

*(no reviewed lessons yet — nothing is injected while this file has no entries)*
